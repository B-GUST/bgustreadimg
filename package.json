{
  "name": "bgustreadimg",
  "version": "0.1.4",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "bgustreadimg",
    "triples": {
      "additional": [
        "x86_64-unknown-linux-gnu"
      ]
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.15.0"
  }
}
